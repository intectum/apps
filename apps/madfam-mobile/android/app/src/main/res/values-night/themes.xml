<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>

        <item name="colorPrimary">@color/water_bright</item>
        <item name="colorPrimaryVariant">@color/water_bright</item>
        <item name="colorOnPrimary">@color/water_dark</item>

        <item name="colorAccent">@color/water_bright</item>

        <item name="colorOnSurface">@color/stone_light</item>

        <item name="android:colorBackground">@color/stone_dark</item>
        <item name="android:statusBarColor">@color/stone_dark</item>
    </style>

</resources>
