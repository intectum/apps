{
  "name": "trusty-paws-website",
  "scripts": {
    "dev": "next dev",
    "build": "graphql-codegen && next build",
    "start": "next start",
    "deploy": "firebase deploy --only hosting",
    "codegen": "graphql-codegen",
    "codegen-watch": "graphql-codegen --watch"
  }
}
